{{ form_start(form) }}
  <div class="d-flex w-100 mt-2 gap-3">
    <div class="w-100 mb-0">
      {{ form_widget(form) }}
    </div>

    <button class="access-btn mb-2 p-1">{{ button_label|default('submit') }}</button>
  </div>
{{ form_end(form) }}


